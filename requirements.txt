# Core dependencies (always required)
requests>=2.28.0
tabulate>=0.9.0
rich>=13.0.0
flask>=3.0.0

# NFL data dependencies (optional — needed for player/game stats commands)
# Install with: pip install fantasy-football-stats[nfl]
# On Termux/Android, see README.md for special build instructions.
# nfl_data_py>=0.3.0
# pandas>=1.5.0
